grunt-coffee-gamecore-mocha
===

* `coffeescript` for language
* `gamecore` for object pools
* `mocha`, `sinon`, `chai` used for testing
* `blanket` for testing code coverage reports
* `lodash` for utilities
* `commonjs` for modules
